{
  "private": true,
  "scripts": {
    "dev": "next",
    "build": "next build && next export",
    "clean": "rm -rf ./out",
    "deploy": "next build && rm -rf ./out && mkdir out && touch out/.nojekyll && git add out/ && git commit -m \"Deploy\" && git subtree push --prefix out origin gh-pages",
    "add-domain": "echo \"next.varbintech.com\" > ./out/CNAME"
  },
  "dependencies": {
    "next": "latest",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "@types/node": "18.11.5",
    "@types/react": "18.0.23",
    "@types/react-dom": "18.0.7",
    "typescript": "4.8.4"
  }
}

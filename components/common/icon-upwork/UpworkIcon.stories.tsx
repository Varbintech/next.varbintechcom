import { storiesOf } from '@storybook/react';

import UpworkIcon from './UpworkIcon';

storiesOf('App/CommonComponents/Logos/Upwork', module).add('Default', () => (
  <UpworkIcon sx={{ width: '100px', height: '100px' }} />
));

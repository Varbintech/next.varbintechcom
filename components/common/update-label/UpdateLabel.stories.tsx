import { storiesOf } from '@storybook/react';

import UpdateLabel from './UpdateLabel';

storiesOf('App/CommonComponents/UpdateLabel', module).add('Default', () => (
  <UpdateLabel>Update Label</UpdateLabel>
));
